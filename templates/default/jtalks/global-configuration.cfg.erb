# these are default configurations of the projects. Placeholders are taken from script parameters
# you can override these values by creating a similar config in configs/${env}/${env}.cfg or
# configs/${env}/${project}.cfg
[backup]
folder=<%= @jtalks_folder %>/backups
[tomcat]
location=<%= @tomcat_location %>
[app]
final_name=ROOT